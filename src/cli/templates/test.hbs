import { {{pascalCase name}} } from '../../src/{{folder}}/{{pascalCase name}}/{{pascalCase name}}.{{suffix}}';

describe('{{pascalCase name}}', () => {
  it('should render', () => {
    const instance = new {{pascalCase name}}({});
    expect(instance.render()).toBeDefined();
  });
});
