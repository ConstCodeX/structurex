import { {{pascalCase name}}{{base}} } from './{{pascalCase name}}{{suffix}}';
import type { {{pascalCase name}}Props } from './types';

export const {{pascalCase name}} = (props: {{pascalCase name}}Props) => {
  return new {{pascalCase name}}{{base}}(props).render();
};